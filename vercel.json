{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "services": {
    "web": {
      "buildCommand": "npm run build",
      "outputDirectory": "dist"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
